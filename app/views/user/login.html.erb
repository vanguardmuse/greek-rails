<% form_for :user, :url => { :action => "authenticate" } do |f| %>
  <%= f.label :name, "Name" %>
  <%= f.text_field :name %> 
  <%= f.label :password, "Password" %>
  <%= f.password_field :password %>
  <%= submit_tag "Login" %>
<% end %>
