<ul>
  <% @semesters.each do |semester| %>
    <li><%= semester.name %>
      <ul>
        <% semester.users.each do |user| %>
          <li><%= link_to user.name, {:action => :show, :id => user.id } %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
